<!DOCTYPE html>
<html lang="en">
<head>
<link rel="manifest" href="manifest.json">

<meta name="theme-color" content="#000000">

<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black">
<meta name="apple-mobile-web-app-title" content="MyOnlineDrive">
<link rel="apple-touch-icon" href="MyDrive-icon-192.png">

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Online Drive - macOS Edition</title>
    <style>
        /* Base Styles & macOS Background */
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; 
            background: url('https://images.unsplash.com/photo-1614850523459-c2f4c699c52e?q=80&w=2070&auto=format&fit=crop') no-repeat center center fixed;
            background-size: cover;
            margin: 0; 
            padding: 0;
            box-sizing: border-box; 
            height: 100vh;
            overflow: hidden;
        }

        /* macOS Lock Screen Styles */
        #loginSection {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            height: 100vh;
            width: 100vw;
            position: absolute;
            top: 0; left: 0;
            z-index: 50;
            backdrop-filter: blur(5px);
            background: rgba(0,0,0,0.2);
        }
        .profile-pic {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
            border: 2px solid rgba(255, 255, 255, 0.2);
        }
        .user-name {
            color: white;
            font-size: 24px;
            font-weight: 600;
            text-shadow: 0 1px 5px rgba(0,0,0,0.5);
            margin-bottom: 25px;
            margin-top: 0;
        }
        .password-wrapper {
            position: relative;
            display: flex;
            align-items: center;
            width: 240px;
        }
        #passwordInput { 
            width: 100%; 
            padding: 10px 40px 10px 20px; 
            background: rgba(255, 255, 255, 0.2); 
            border: 1px solid rgba(255, 255, 255, 0.3); 
            border-radius: 20px; 
            box-sizing: border-box; 
            font-size: 14px; 
            color: white;
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            outline: none;
            transition: 0.3s;
        }
        #passwordInput::placeholder { color: rgba(255, 255, 255, 0.7); }
        #passwordInput:focus {
            background: rgba(255, 255, 255, 0.3);
            border-color: rgba(255, 255, 255, 0.5);
        }
        .submit-arrow {
            position: absolute;
            right: 8px;
            background: rgba(255, 255, 255, 0.2);
            border: none;
            color: white;
            width: 24px;
            height: 24px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: 0.3s;
        }
        .submit-arrow:hover { background: rgba(255, 255, 255, 0.4); }
        .error-msg { 
            color: #ff6b6b; 
            font-size: 14px; 
            margin-bottom: 15px; 
            display: none; 
            text-shadow: 0 1px 3px rgba(0,0,0,0.8);
            background: rgba(0,0,0,0.5);
            padding: 5px 10px;
            border-radius: 10px;
        }

        /* macOS Desktop & Dock Styles */
        #desktopSection {
            display: none;
            height: 100vh;
            width: 100vw;
            position: relative;
        }
        
        /* Top Menu Bar */
        .mac-topbar {
            height: 28px;
            background: rgba(255, 255, 255, 0.4);
            backdrop-filter: blur(20px);
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
            font-size: 13px;
            font-weight: 500;
            color: #000;
            box-shadow: 0 1px 2px rgba(0,0,0,0.1);
        }
        .topbar-left, .topbar-right {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        .topbar-left b { font-weight: 600; }

        /* Bottom Dock */
        .mac-dock-container {
            position: absolute;
            bottom: 15px;
            width: 100%;
            display: flex;
            justify-content: center;
            z-index: 20;
        }
        .mac-dock {
            background: rgba(255, 255, 255, 0.25);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid rgba(255, 255, 255, 0.4);
            border-radius: 24px;
            padding: 8px 12px;
            display: flex;
            gap: 10px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }
        .dock-icon-wrapper {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .dock-icon {
            width: 50px;
            height: 50px;
            cursor: pointer;
            transition: all 0.2s cubic-bezier(0.25, 0.8, 0.25, 1);
            border-radius: 12px;
        }
        .dock-icon:hover {
            transform: scale(1.3) translateY(-10px);
            margin: 0 10px;
        }
        .dock-dot {
            width: 4px;
            height: 4px;
            background: rgba(0,0,0,0.5);
            border-radius: 50%;
            margin-top: 5px;
            display: none;
        }
        .dock-icon-wrapper.active .dock-dot {
            display: block;
        }

        @keyframes appBounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }
        .bouncing { animation: appBounce 0.5s ease-in-out; }

        /* macOS Window Container */
        .container { 
            background: rgba(245, 245, 247, 0.9); 
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            width: 100%; 
            max-width: 700px; 
            border-radius: 12px; 
            box-shadow: 0 20px 50px rgba(0,0,0,0.3), 0 0 0 1px rgba(255,255,255,0.4); 
            box-sizing: border-box; 
            overflow: hidden; 
            position: absolute;
            top: 50%;
            left: 50%;
        }
        
        /* Blocked Section (Centered static) */
        #blockedSection {
            display: none;
            transform: translate(-50%, -50%);
            z-index: 100;
        }

        /* Drive Window (Animated hidden/show) */
        #driveSection {
            visibility: hidden;
            opacity: 0;
            transform: translate(-50%, -50%) scale(0.8);
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            z-index: 10;
        }
        #driveSection.app-active {
            visibility: visible;
            opacity: 1;
            transform: translate(-50%, -50%) scale(1);
        }

        /* macOS Title Bar */
        .mac-titlebar {
            background: rgba(225, 225, 225, 0.8);
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
            padding: 12px 16px;
            display: flex;
            align-items: center;
            position: relative;
        }
        .mac-dots {
            display: flex;
            gap: 8px;
        }
        .dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            cursor: pointer;
        }
        .dot.red { background-color: #ff5f56; border: 1px solid #e0443e; }
        .dot.yellow { background-color: #ffbd2e; border: 1px solid #dea123; }
        .dot.green { background-color: #27c93f; border: 1px solid #1aab29; }
        
        .dot.red:hover { background-color: #ff4b41; }
        .dot.yellow:hover { background-color: #ffab0f; }
        .dot.green:hover { background-color: #1dc435; }

        .mac-title {
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            font-size: 13px;
            font-weight: 600;
            color: #4b5563;
        }

        .drive-content { padding: 20px 30px 30px 30px; }
        #blockedSection .drive-content { text-align: center; padding: 40px; }
        #countdown { font-size: 24px; font-weight: bold; color: #ef4444; margin-top: 15px; }

        /* macOS Drive Elements */
        .search-container { position: relative; margin-bottom: 25px; width: 100%; }
        .search-icon { position: absolute; left: 12px; top: 50%; transform: translateY(-50%); width: 16px; height: 16px; color: #8e8e93; }
        #searchInput { 
            width: 100%; 
            padding: 8px 15px 8px 35px; 
            background: rgba(0, 0, 0, 0.05); 
            border: 1px solid rgba(0, 0, 0, 0.05); 
            border-radius: 8px; 
            box-sizing: border-box; 
            font-size: 14px; 
            outline: none;
            transition: 0.2s;
        }
        #searchInput:focus {
            background: white;
            border-color: #007aff;
            box-shadow: 0 0 0 3px rgba(0, 122, 255, 0.2);
        }

        .file-list { display: flex; flex-direction: column; gap: 4px; }
        .file-card { 
            padding: 10px 12px; 
            border-radius: 6px; 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            transition: background 0.1s; 
            border: none;
            background: transparent;
        }
        .folder-card { cursor: pointer; }
        .file-card:hover { background: rgba(0, 122, 255, 0.1); }
        .file-card:active { background: rgba(0, 122, 255, 0.2); }
        
        .file-info { display: flex; align-items: center; gap: 12px; }
        .folder-icon, .file-icon { width: 28px; height: 28px; flex-shrink: 0; }
        .folder-icon { fill: #007aff !important; } 
        .file-icon { color: #8e8e93; }
        .file-info h3 { margin: 0; color: #1c1c1e; font-size: 14px; font-weight: 500; }
        
        .download-icon-btn { 
            color: #007aff; 
            background: none; 
            border: none; 
            cursor: pointer; 
            padding: 6px; 
            border-radius: 50%; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            transition: 0.2s; 
            opacity: 0; 
        }
        .file-card:hover .download-icon-btn { opacity: 1; }
        .download-icon-btn:hover { background: rgba(0, 122, 255, 0.1); }
        .download-svg { width: 20px; height: 20px; }
        
        .back-btn { 
            display: flex; 
            align-items: center; 
            gap: 6px; 
            cursor: pointer; 
            color: #007aff; 
            margin-bottom: 20px; 
            font-weight: 500; 
            width: fit-content; 
            padding: 4px 8px; 
            border-radius: 6px; 
            transition: 0.2s; 
            font-size: 14px;
            margin-left: -8px; 
        }
        .back-btn:hover { background: rgba(0, 122, 255, 0.1); }
        .back-icon { width: 16px; height: 16px; }
    </style>
</head>
<body oncontextmenu="return false">

    <div id="loginSection">
        <img src="https://i.ibb.co.com/355Qy8KY/cropped-circle-image.png" alt="User Profile" class="profile-pic">
        <h2 class="user-name">Wasim Mahmood</h2>
        <p id="errorMsg" class="error-msg"></p>
        <div class="password-wrapper">
            <input type="password" id="passwordInput" placeholder="Enter Password">
            <button class="submit-arrow" onclick="checkPassword()">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" width="16" height="16">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M14 5l7 7m0 0l-7 7m7-7H3" />
                </svg>
            </button>
        </div>
    </div>

    <div id="desktopSection">
        
        <div class="mac-topbar">
            <div class="topbar-left">
                <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 384 512" fill="#000"><path d="M318.7 268.7c-.2-36.7 16.4-64.4 50-84.8-18.8-26.9-47.2-41.7-84.7-44.6-35.5-2.8-74.3 20.7-88.5 20.7-15 0-49.4-19.7-76.4-19.7C63.3 141.2 4 184.8 4 273.5q0 39.3 14.4 81.2c12.8 36.7 59 126.7 107.2 125.2 25.2-.6 43-17.9 75.8-17.9 31.8 0 48.3 17.9 76.4 17.3 48.6-.8 90.4-91.2 102.7-124.5-56-25-82.6-76.3-81.8-86.1zM242.2 87.2C259.6 65.5 269 36.3 266 7.4c-25.2 2-55.5 17.6-73.4 39.1-16.1 19.3-27.4 49-23.4 76.9 27.8 2.2 55.7-14.5 73-36.2z"/></svg>
                <b>Finder</b>
                <span>File</span>
                <span>Edit</span>
                <span>View</span>
                <span>Go</span>
                <span>Window</span>
                <span>Help</span>
            </div>
            <div class="topbar-right">
                <span id="macClock">10:00 AM</span>
            </div>
        </div>

        <div class="container" id="driveSection">
            <div class="mac-titlebar">
                <div class="mac-dots">
                    <div class="dot red" onclick="closeFinder()"></div>
                    <div class="dot yellow" onclick="closeFinder()"></div>
                    <div class="dot green"></div>
                </div>
                <div class="mac-title">My Online Drive</div>
            </div>

            <div class="drive-content">
                <div class="search-container">
                    <svg class="search-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                    </svg>
                    <input type="text" id="searchInput" placeholder="Search" onkeyup="searchFiles()">
                </div>
                
                <div id="backButtonContainer" style="display: none;">
                    <div class="back-btn" onclick="openFolder('root')">
                        <svg class="back-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
                        </svg>
                        Back
                    </div>
                </div>

                <div class="file-list" id="fileList">
                    <p style="text-align:center; color:#8e8e93; font-size: 14px;">Loading...</p>
                </div>
            </div>
        </div>

        <div class="mac-dock-container">
            <div class="mac-dock">
                <div class="dock-icon-wrapper" id="finderWrapper">
                    <img src="https://i.ibb.co.com/YF02Rh93/finder-macos-bigsur-icon-190173.png" alt="Finder" class="dock-icon" onclick="openFinder(this)">
                    <div class="dock-dot"></div>
                </div>
                <div class="dock-icon-wrapper">
                    <img src="https://i.ibb.co.com/jk3WbBby/safari-macos-bigsur-icon-189770.png" alt="Safari" class="dock-icon">
                    <div class="dock-dot"></div>
                </div>
                <div class="dock-icon-wrapper">
                    <img src="https://i.ibb.co.com/wh7c0QPB/mail-macos-bigsur-icon-190003.png" alt="Mail" class="dock-icon">
                </div>
                <div class="dock-icon-wrapper">
                    <img src="https://i.ibb.co.com/M5f2fr0v/google-drive-macos-bigsur-icon-190126.png" alt="Google Drive" class="dock-icon">
                </div>
                <div class="dock-icon-wrapper">
                    <img src="https://i.ibb.co.com/rfG5kYtM/settings-macos-bigsur-icon-189754.png" alt="Settings" class="dock-icon">
                </div>
            </div>
        </div>

    </div>

    <div class="container" id="blockedSection">
        <div class="mac-titlebar">
            <div class="mac-dots">
                <div class="dot red"></div>
                <div class="dot yellow"></div>
                <div class="dot green"></div>
            </div>
            <div class="mac-title">System Alert</div>
        </div>
        <div class="drive-content">
            <h2 style="color: #ef4444; margin-top:0;">Access Blocked</h2>
            <div id="countdown"></div>
        </div>
    </div>

    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js";
        import { getDatabase, ref, onValue } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-database.js";

        const firebaseConfig = {
            apiKey: "AIzaSyAAgePbRLUBux1hh_UpavZ442sPYULAN-g",
            authDomain: "myonlinedrive-9b428.firebaseapp.com",
            databaseURL: "https://myonlinedrive-9b428-default-rtdb.firebaseio.com",
            projectId: "myonlinedrive-9b428",
            storageBucket: "myonlinedrive-9b428.firebasestorage.app",
            messagingSenderId: "164675746629",
            appId: "1:164675746629:web:ec95a02b26e5f23eb65c2c"
        };

        const app = initializeApp(firebaseConfig);
        const db = getDatabase(app);

        let currentPath = "root";
        let driveFolders = [];
        let driveFiles = {};

        // Security Constants (Obfuscated Token)
        const AUTH_TOKEN_KEY = '_mac_os_sec_uuid';
        const VALID_TOKEN = 'x9f2a_042567_auth';

        onValue(ref(db, 'driveData'), (snapshot) => {
            const data = snapshot.val();
            if(data) {
                driveFolders = data.folders ? Object.values(data.folders) : [];
                driveFiles = data.files || {};
                if(sessionStorage.getItem(AUTH_TOKEN_KEY) === VALID_TOKEN) {
                    renderDrive();
                }
            } else {
                document.getElementById("fileList").innerHTML = "<p style='text-align:center; color:#8e8e93; font-size: 14px;'>No folders found. Add from Admin Panel.</p>";
            }
        });

        window.renderDrive = function() {
            const listContainer = document.getElementById("fileList");
            const backBtn = document.getElementById("backButtonContainer");
            const searchInput = document.getElementById("searchInput");
            
            listContainer.innerHTML = "";
            searchInput.value = ""; 

            if (currentPath === "root") {
                backBtn.style.display = "none";
                driveFolders.forEach(folder => {
                    listContainer.innerHTML += `
                        <div class="file-card folder-card" onclick="openFolder('${folder.id}')">
                            <div class="file-info">
                                <svg class="folder-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
                                    <path d="M10 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V8c0-1.1-.9-2-2-2h-8l-2-2z"/>
                                </svg>
                                <h3>${folder.name}</h3>
                            </div>
                        </div>`;
                });
            } else {
                backBtn.style.display = "block";
                const filesInFolder = driveFiles[currentPath] ? Object.values(driveFiles[currentPath]) : [];
                
                if(filesInFolder.length === 0) {
                    listContainer.innerHTML = "<p style='text-align:center; color:#8e8e93; font-size: 14px;'>Folder is empty.</p>";
                }

                filesInFolder.forEach(file => {
                    listContainer.innerHTML += `
                        <div class="file-card">
                            <div class="file-info">
                                <svg class="file-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
                                </svg>
                                <h3>${file.name}</h3>
                            </div>
                            <button class="download-icon-btn" onclick="openSecureLink('${file.link}')" title="Download">
                                <svg class="download-svg" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
                                </svg>
                            </button>
                        </div>`;
                });
            }
        };

        window.openFolder = function(folderId) {
            currentPath = folderId;
            renderDrive();
        };

        window.searchFiles = function() {
            let input = document.getElementById('searchInput').value.toLowerCase();
            let cards = document.getElementsByClassName('file-card');
            for (let i = 0; i < cards.length; i++) {
                let title = cards[i].getElementsByTagName('h3')[0].innerText.toLowerCase();
                cards[i].style.display = title.includes(input) ? "flex" : "none";
            }
        };

        window.openSecureLink = function(encodedUrl) {
            let decodedUrl = atob(encodedUrl);
            window.open(decodedUrl, '_blank');
        };

        /* macOS Window Logic */
        window.openFinder = function(element) {
            if(element) {
                element.classList.add('bouncing');
                setTimeout(() => element.classList.remove('bouncing'), 500);
            }
            setTimeout(() => {
                document.getElementById('driveSection').classList.add('app-active');
                document.getElementById('finderWrapper').classList.add('active');
            }, 200);
        };

        window.closeFinder = function() {
            document.getElementById('driveSection').classList.remove('app-active');
            document.getElementById('finderWrapper').classList.remove('active');
        };

        /* Live Clock for Topbar */
        setInterval(() => {
            let date = new Date();
            let hours = date.getHours();
            let minutes = date.getMinutes();
            let ampm = hours >= 12 ? 'PM' : 'AM';
            hours = hours % 12;
            hours = hours ? hours : 12; 
            minutes = minutes < 10 ? '0' + minutes : minutes;
            document.getElementById('macClock').innerText = hours + ':' + minutes + ' ' + ampm;
        }, 1000);

        // Advanced Client Security 
        const BLOCK_DURATION = 10 * 60 * 60 * 1000; 
        
        // Anti-debugging
        document.addEventListener('contextmenu', event => event.preventDefault());
        document.onkeydown = function(e) {
            if (e.keyCode == 123) return false; 
            if (e.ctrlKey && e.shiftKey && (e.keyCode === 73 || e.keyCode === 74 || e.keyCode === 67)) return false; 
            if (e.ctrlKey && e.keyCode === 85) return false; 
        };

        function initSecurity() {
            let blockTime = localStorage.getItem('driveBlockUntil');
            if (blockTime && Date.now() < parseInt(blockTime)) {
                showBlocked(parseInt(blockTime));
            } else {
                localStorage.removeItem('driveBlockUntil');
                if (sessionStorage.getItem(AUTH_TOKEN_KEY) === VALID_TOKEN) {
                    document.getElementById('loginSection').style.display = 'none';
                    document.getElementById('desktopSection').style.display = 'block';
                    setTimeout(() => openFinder(document.querySelector('#finderWrapper .dock-icon')), 500);
                }
            }
        }

        // Obfuscated Password Generator Function
        const _getSecret = () => {
            const _p1 = ['0', '4', '2'];
            const _p2 = ['5', '6', '7'];
            const _p3 = ['m', 'm'];
            return _p1.join('') + _p2.join('') + _p3.join('');
        };

        window.checkPassword = function() {
            let input = document.getElementById('passwordInput').value;
            let errorMsg = document.getElementById('errorMsg');
            let attempts = parseInt(localStorage.getItem('driveAttempts') || '0');

            // Compare with the securely reconstructed string
            if (input === _getSecret()) {
                sessionStorage.setItem(AUTH_TOKEN_KEY, VALID_TOKEN);
                localStorage.removeItem('driveAttempts');
                document.getElementById('loginSection').style.display = 'none';
                document.getElementById('desktopSection').style.display = 'block';
                renderDrive();
                setTimeout(() => openFinder(document.querySelector('#finderWrapper .dock-icon')), 500);
            } else {
                attempts++;
                if (attempts >= 3) {
                    let blockUntil = Date.now() + BLOCK_DURATION;
                    localStorage.setItem('driveBlockUntil', blockUntil);
                    localStorage.removeItem('driveAttempts');
                    showBlocked(blockUntil);
                } else {
                    localStorage.setItem('driveAttempts', attempts);
                    errorMsg.innerText = `Incorrect Password! ${3 - attempts} attempts left.`;
                    errorMsg.style.display = 'block';
                }
            }
        };

        function showBlocked(blockTime) {
            document.getElementById('loginSection').style.display = 'none';
            document.getElementById('desktopSection').style.display = 'none';
            document.getElementById('blockedSection').style.display = 'block';
        }

        document.getElementById('passwordInput').addEventListener('keypress', function (e) {
            if (e.key === 'Enter') checkPassword();
        });

        window.onload = initSecurity;
    </script>
<script>
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', () => {
      navigator.serviceWorker.register('/sw.js')
        .then(reg => console.log('Service Worker Registered!', reg))
        .catch(err => console.log('Service Worker Registration Failed!', err));
    });
  }
</script>
</body>
</html>


Website e dekle auto translate hoye jai. Eta remove koro. Ar ekane je file dekase kinto ami kon folder teke ekane asesi eta dekai na, eta add kore diab. Ar folder er bitor file gola jei formet e take na keno jemon 
apk
xapk
apks
aab
ipa
exe
msi
dmg
pkg
appx
img
iso
bin
nrg
vhd
vmdk
jpg
jpeg
png
gif
webp
bmp
tiff
svg
ico
heic
raw
psd
ai
eps
pdf
doc
docx
txt
rtf
odt
xls
xlsx
ppt
pptx
csv
md
log
mp3
wav
aac
flac
ogg
m4a
wma
amr
mp4
mkv
avi
mov
wmv
flv
webm
3gp
mpeg
mpg
zip
rar
7z
tar
gz
bz2
xz
html
htm
css
js
json
xml
php
py
java
c
cpp
cs
rb
go
swift
kt
ts
dart
sql
db
sqlite
bak
tmp
config
ini
yaml
yml
apkx
obb
pak
dat
dll
sys
bat
sh

Sob golar icon macOS er moto takbe real tar moto. Ar nise dock e safari te click korle same macOS safari er moto  tab oepn hobe, Scharch bar e Scharch diel amar dribe je folder/file ase click korle khoje ber korbe. Ar finder teke Scharch bar ta remove kore diba. Je safari ta open hoba eta jeno dekte same macOS er safari er moto lage.  Ar protm bar login korle auto finder open hoy. Eta off kore diab. Ar top bar e finder e click korle finder open hobe, file e click kirle alada Desktop

Applications

Downloads

AirDrop

iCloud Drive

Dropbox

Documents

Recents

Blog Stuff

Pictures

Design Assets

Blog Image Archive

Google Drive

Movies

Eigoal dekabe masOS er moto icon soho. Then Applications e click korle joto gola app ase ei drive e sob gola ase jabe. Abar macOS er moto Scharch bar o takbe. Desktop

Applications

Downloads

AirDrop

iCloud Drive

Dropbox

Documents

Recents

Blog Stuff

Pictures

Design Assets

Blog Image Archive

Google Drive

Movies

Sob golar jonno same oi. 

Tar por Edit e click korle shoja ei website e niye jabe https://my-drive-arm.netlify.app/admin.html
 
Ja ja bolsi ekon ta ta add kore deo. Ar code er baki kono kiso change korba na please.